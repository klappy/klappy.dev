# Odd Teaser — Attempt Kickoff

Build a thinking companion with real Claude API. User types freely, LLM detects artifacts (learning/decision/override), surfaces for consent, exports to Markdown.

## Process guards

- Write to `products/odd-teaser/src/` — branch is the gate
- Create `products/odd-teaser/index.html` for Vite
- Extract JS to `.js` files
- Commit screenshots to repo

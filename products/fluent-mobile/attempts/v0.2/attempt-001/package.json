{
  "name": "fluent-mobile-poc-v02",
  "version": "0.2.0",
  "description": "Fluent Mobile PoC - v0.2 with waveform visualization and multi-screen navigation",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev src --port 8788",
    "deploy": "npx wrangler pages deploy src --project-name=fluent-mobile-poc",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "wrangler": "^3.0.0"
  }
}

#!/usr/bin/env sh

# Generate README index tables from frontmatter.
# See: docs/planning/automated-readme-indexes.md
node scripts/generate-indexes.mjs

# Stage any READMEs that were updated by the generator.
git diff --name-only -- '*/README.md' | xargs -r git add
